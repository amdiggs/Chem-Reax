This folder contains a the source files and makefile to build REAX which will run a stochastic differential equation.


The ChemReax Exacutable requires four command line arguments
-out followed by the desired name of the out file.
-in followed by the name of an input file formated like the given example.

 

